
import React from "react";
import ToolbarManager from "../components/ToolbarManager";

const Index = () => {
  return (
    <div className="min-h-screen bg-gradient-to-b from-dysaccess-light-blue to-white relative">
      {/* La barre d'outils flottante */}
      <ToolbarManager />
    </div>
  );
};

export default Index;
